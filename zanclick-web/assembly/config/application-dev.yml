spring:
  datasource:
    username: prepay
    password: Prepay2019.
    url: jdbc:mysql://localhost:13306/zanclick_prepay?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 30000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      web-stat-filter:
        enabled: true
        url-pattern: '/*'
        exclusions: '*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*'
        session-stat-enable: true
      stat-view-servlet:
        url-pattern: '/druid/*'
        reset-enable: false
        login-username: zanclick
        login-password: ZanClick666
        enabled: true
      filter:
        stat:
          slow-sql-millis: 5000
  redis:
    host: 127.0.0.1
    port: 16379
    password: zanclick2019
    timeout: 3000ms
    database: 6
    lettuce:
      pool:
        max-wait: 2000ms
        min-idle: 50
        max-idle: 100
        max-active: 200

#预授权通知接口
authorize:
  notify: http://112.35.84.241:18080/api/open/authorize/notify

# H5接口访问密钥
h5:
  key: 12345678qwertyui

 #支付超时时间
timeout:
  authorize:
    prepay: 10